"use strict";
exports.__esModule = true;
var cache_1 = require("./cache");
cache_1.createConfigDir();
jest.useFakeTimers().setSystemTime(new Date('2022-01-01'));
var fakeTime = new Date('2022-01-01').getTime();
test('can save update then get the update details', function () {
    cache_1.saveLastUpdate('test');
    expect(cache_1.getLastUpdate('test')).toBe(fakeTime);
});
test('prefixed module can save update then get the update details', function () {
    cache_1.saveLastUpdate('@alexbrazier/test');
    expect(cache_1.getLastUpdate('@alexbrazier/test')).toBe(fakeTime);
});
